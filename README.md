# contravis4

Contra dance choreography visualizer and animator. Design contra dances, see them animated, and export as GIFs.

## Development

```sh
npm install
npm run dev        # start dev server
npm run test       # run tests once
npm run test:watch # run tests in watch mode
npm run build      # typecheck + production build
```
