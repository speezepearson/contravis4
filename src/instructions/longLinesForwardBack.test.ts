import { describe } from "vitest";

describe.skip("longLinesForwardBack", () => {
  // Tests removed: they tested the legacy longLinesForwardBackSegments API
  // which has been removed in favor of longLinesForwardBackAnimator.
});
