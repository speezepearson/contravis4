import { describe } from "vitest";

describe.skip("takeHandsInRings", () => {
  // Tests removed: they tested the legacy takeHandsInRingsSegments API
  // which has been removed in favor of takeHandsInRingsAnimator.
});
